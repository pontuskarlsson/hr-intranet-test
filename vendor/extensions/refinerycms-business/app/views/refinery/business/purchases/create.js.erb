var stripe = Stripe('<%= ENV['STRIPE_PUBLISHABLE_KEY'] %>');

stripe.redirectToCheckout({
  sessionId: '<%= @purchase_form.purchase&.stripe_checkout_session_id %>'
}).then(function (result) {
  <% if Rails.env.development? %>
  console.log({result: result})
  <% end %>
});
