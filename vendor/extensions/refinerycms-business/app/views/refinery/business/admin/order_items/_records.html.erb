<%= render 'refinery/admin/search_header', :url => refinery.business_admin_order_items_path %>
<div class='pagination_container'>
  <% if @order_items.any? %>
    <%= render 'order_items' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
