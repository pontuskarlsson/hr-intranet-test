<%= render 'refinery/admin/search_header', :url => refinery.business_admin_invoices_path %>
<div class='pagination_container'>
  <% if @invoices.any? %>
    <%= render 'invoices' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
