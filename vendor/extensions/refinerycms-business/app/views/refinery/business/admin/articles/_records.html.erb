<%= render 'refinery/admin/search_header', :url => refinery.business_admin_articles_path %>
<div class='pagination_container'>
  <% if @articles.any? %>
    <%= render 'articles' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
