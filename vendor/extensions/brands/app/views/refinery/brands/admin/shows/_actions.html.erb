<ul>
  <% if ::Refinery::Brands::Admin::ShowsController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.brands_admin_shows_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_brands_admin_show_path,
                 :class => "add_icon" %>
  </li>
  <%= render 'refinery/brands/admin/shared/links' %>
<% if !searching? && ::Refinery::Brands::Admin::ShowsController.sortable? && ::Refinery::Brands::Show.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Shows"),
                 refinery.brands_admin_shows_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Shows"),
                 refinery.brands_admin_shows_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
