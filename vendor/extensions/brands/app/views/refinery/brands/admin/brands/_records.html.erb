<%= render 'refinery/admin/search_header', :url => refinery.brands_admin_brands_path %>
<div class='pagination_container'>
  <% if @brands.any? %>
    <%= render 'brands' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
