<ul>
  <% if ::Refinery::Parcels::Admin::ShipmentsController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.parcels_admin_shipments_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_parcels_admin_shipment_path,
                 :class => "add_icon" %>
  </li>
  <%= render '/refinery/parcels/admin/shared/links' %>

<% if !searching? && ::Refinery::Parcels::Admin::ShipmentsController.sortable? && ::Refinery::Parcels::Shipment.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Shipments"),
                 refinery.parcels_admin_shipments_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Shipments"),
                 refinery.parcels_admin_shipments_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
