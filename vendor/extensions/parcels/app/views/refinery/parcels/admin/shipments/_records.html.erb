<%= render 'refinery/admin/search_header', :url => refinery.parcels_admin_shipments_path %>
<div class='pagination_container'>
  <% if @shipments.any? %>
    <%= render 'shipments' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
