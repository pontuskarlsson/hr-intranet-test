<%= render 'refinery/admin/search_header', :url => refinery.employees_admin_public_holidays_path %>
<div class='pagination_container'>
  <% if @public_holidays.any? %>
    <%= render 'public_holidays' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
