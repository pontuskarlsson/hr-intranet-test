<% content_for :body do %>

  <%= render 'table', table_id: 'js_inspections_data_table', options: {}, inspections: @inspections, ajax: refinery.quality_assurance_inspections_path(filter_params) %>

<% end %>

<%= render '/refinery/content_page' %>
