<ul>
  <% if ::Refinery::QualityAssurance::Admin::InspectionsController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.quality_assurance_admin_inspections_path %>
    </li>
  <% end %>
  <li>
    <%= action_label :add, refinery.new_quality_assurance_admin_inspection_path, t('.create_new') %>
  </li>
<% if !searching? && ::Refinery::QualityAssurance::Admin::InspectionsController.sortable? && ::Refinery::QualityAssurance::Inspection.many? %>
  <li>
    <%= action_label :reorder,
                     refinery.quality_assurance_admin_inspections_path,
                     t('.reorder', what:  "Inspections"), id: 'reorder_action'%>
    <%= action_label :reorder_done,
                     refinery.quality_assurance_admin_inspections_path,
                      t('.reorder_done', what: "Inspections"), id: 'reorder_action_done', class: 'hidden'%>
  </li>
<% end %>
</ul>
