<%= render 'refinery/admin/search_header', :url => refinery.quality_assurance_admin_inspections_path %>
<div class='pagination_container'>
  <% if @inspections.any? %>
    <%= render 'inspections' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
