<ul>
  <% if ::Refinery::Store::Admin::OrdersController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.store_admin_orders_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_store_admin_order_path,
                 :class => "add_icon" %>
  </li>
  <%= render '/refinery/store/admin/shared/links' %>

<% if !searching? && ::Refinery::Store::Admin::OrdersController.sortable? && ::Refinery::Store::Order.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Orders"),
                 refinery.store_admin_orders_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Orders"),
                 refinery.store_admin_orders_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
