<%= render 'refinery/admin/search_header', :url => refinery.custom_lists_admin_custom_lists_path %>
<div class='pagination_container'>
  <% if @custom_lists.any? %>
    <%= render 'custom_lists' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
