<ul>
  <% if ::Refinery::Employees::Admin::AnnualLeavesController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.employees_admin_annual_leaves_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_employees_admin_annual_leave_path,
                :class => "add_icon" %>
  </li>
  <%= render '/refinery/employees/admin/shared/links' %>

<% if !searching? && ::Refinery::Employees::Admin::AnnualLeavesController.sortable? && ::Refinery::Employees::AnnualLeave.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Annual Leaves"),
                 refinery.employees_admin_annual_leaves_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Annual Leaves"),
                 refinery.employees_admin_annual_leaves_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
