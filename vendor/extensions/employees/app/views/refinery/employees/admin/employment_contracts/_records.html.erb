<%= render 'refinery/admin/search_header', :url => refinery.employees_admin_employment_contracts_path %>
<div class='pagination_container'>
  <% if @employment_contracts.any? %>
    <%= render 'employment_contracts' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
