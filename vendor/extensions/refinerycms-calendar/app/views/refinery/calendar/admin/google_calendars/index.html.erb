<section id='records'>
  <h2>Google Calendars</h2>
  <%= render 'records' %>
</section>
<aside id='actions'>
  <%= render 'actions' %>
</aside>
<%= render '/refinery/admin/make_sortable', :tree => false if !searching? and ::Refinery::Calendar::Admin::GoogleCalendarsController.sortable? and ::Refinery::Calendar::GoogleCalendar.count > 1 %>
