<ul>
  <% if ::Refinery::Calendar::Admin::GoogleCalendarsController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.calendar_admin_google_calendars_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_calendar_admin_google_calendar_path,
                 :class => "add_icon" %>
  </li>
  <%= render 'refinery/calendar/admin/shared/links' %>
<% if !searching? && ::Refinery::Calendar::Admin::GoogleCalendarsController.sortable? && ::Refinery::Calendar::GoogleCalendar.any? %>
  <li>
    <%= link_to t('.reorder', :what => "Google Calendars"),
                 refinery.calendar_admin_google_calendars_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Google Calendar"),
                 refinery.calendar_admin_google_calendars_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
