<li class='clearfix record <%= cycle("on", "on-hover") %>' id="<%= dom_id(item) -%>">
  <span class='title'>
    <%= item.to_address_name %>
    <span class="preview">
      <%= t('.preview', from_address_name: item.from_address_name, bill_to: item.bill_to, status: item.status) %>
    </span>
  </span>
  <span class='actions'>
    
    <%= link_to refinery_icon_tag("application_go.png"), refinery.shipping_item_path(item),
        :title => t('.view_live_html'),
        :target => "_blank" %>
    
    <%= link_to refinery_icon_tag("application_edit.png"), refinery.edit_shipping_admin_item_path(item),
         :title => t('.edit') %>
    <%= link_to refinery_icon_tag("delete.png"), refinery.shipping_admin_item_path(item),
        :class => "cancel confirm-delete",
        :title => t('.delete'),
        :confirm => t('message', :scope => 'refinery.admin.delete', :title => item.to_address.try(:name)),
        :method => :delete %>
  </span>
</li>
