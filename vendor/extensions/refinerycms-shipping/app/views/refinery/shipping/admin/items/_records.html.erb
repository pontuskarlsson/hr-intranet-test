<%= render 'refinery/admin/search_header', :url => refinery.shipping_admin_items_path %>
<div class='pagination_container'>
  <% if @items.any? %>
    <%= render 'items' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
