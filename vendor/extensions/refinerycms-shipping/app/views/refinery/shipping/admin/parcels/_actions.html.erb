<ul>
  <% if ::Refinery::Shipping::Admin::ParcelsController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.shipping_admin_parcels_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_shipping_admin_parcel_path,
                 :class => "add_icon" %>
  </li>
  <%= render '/refinery/shipping/admin/shared/links' %>

<% if !searching? && ::Refinery::Shipping::Admin::ParcelsController.sortable? && ::Refinery::Shipping::Parcel.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Parcels"),
                 refinery.shipping_admin_parcels_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Parcels"),
                 refinery.shipping_admin_parcels_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
