<ul>
  <% if ::Refinery::Shipping::Admin::ShipmentAccountsController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.shipping_admin_shipment_accounts_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_shipping_admin_shipment_account_path,
                 :class => "add_icon" %>
  </li>
  <%= render '/refinery/shipping/admin/shared/links' %>

<% if !searching? && ::Refinery::Shipping::Admin::ShipmentAccountsController.sortable? && ::Refinery::Shipping::ShipmentAccount.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Shipment Accounts"),
                 refinery.shipping_admin_shipment_accounts_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Shipment Accounts"),
                 refinery.shipping_admin_shipment_accounts_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
