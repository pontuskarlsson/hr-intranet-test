<ul>
  <% if ::Refinery::Shipping::Admin::ShipmentsController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.shipping_admin_shipments_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_shipping_admin_shipment_path,
                 :class => "add_icon" %>
  </li>
  <%= render '/refinery/shipping/admin/shared/links' %>

<% if !searching? && ::Refinery::Shipping::Admin::ShipmentsController.sortable? && ::Refinery::Shipping::Shipment.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Shipments"),
                 refinery.shipping_admin_shipments_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Shipments"),
                 refinery.shipping_admin_shipments_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
