<ul>
  <% if ::Refinery::Shipping::Admin::PackagesController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.shipping_admin_packages_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_shipping_admin_package_path,
                 :class => "add_icon" %>
  </li>
  <%= render '/refinery/shipping/admin/shared/links' %>

<% if !searching? && ::Refinery::Shipping::Admin::PackagesController.sortable? && ::Refinery::Shipping::Package.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Packages"),
                 refinery.shipping_admin_packages_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Packages"),
                 refinery.shipping_admin_packages_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
