<section class="contact-card">
  <h1 class="cc-attribute cc-name">
    <%=raw contact.name %>
    <i class="fa fa-building"></i>
    <%=raw contact.code %>
  </h1>

  <% if contact.tags_joined_by_comma.present? %>
    <p class="cc-attribute cc-tags">
      <% contact.tags_joined_by_comma.split(',').each do |tag| %>
        <span class="cc-tag"><%= tag %></span>
      <% end %>
    </p>
  <% end %>

  <%= render 'refinery/marketing/contacts/contact_info', contact: contact %>
  <%= render 'refinery/marketing/contacts/address', contact: contact %>
  <%= render 'refinery/marketing/contacts/courier_details', contact: contact %>
  <%= render 'refinery/marketing/contacts/employees', contact: contact %>
</section>
