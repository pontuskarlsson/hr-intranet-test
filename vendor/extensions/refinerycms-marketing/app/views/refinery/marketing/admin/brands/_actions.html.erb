<ul>
  <% if ::Refinery::Marketing::Admin::BrandsController.searchable? %>
    <li>
      <%= render '/refinery/admin/search', :url => refinery.marketing_admin_brands_path %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), refinery.new_marketing_admin_brand_path,
                 :class => "add_icon" %>
  </li>
  <%= render 'refinery/marketing/admin/shared/links' %>
<% if !searching? && ::Refinery::Marketing::Admin::BrandsController.sortable? && ::Refinery::Marketing::Brand.many? %>
  <li>
    <%= link_to t('.reorder', :what => "Brands"),
                 refinery.marketing_admin_brands_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Brands"),
                 refinery.marketing_admin_brands_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
