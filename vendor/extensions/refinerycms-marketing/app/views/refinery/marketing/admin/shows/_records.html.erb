<%= render 'refinery/admin/search_header', :url => refinery.marketing_admin_shows_path %>
<div class='pagination_container'>
  <% if @shows.any? %>
    <%= render 'shows' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
