<%= form_for [refinery, :marketing_admin, @landing_page] do |f| -%>
  <%= render '/refinery/admin/error_messages',
              :object => @landing_page,
              :include_object_name => true %>

  <div class='field'>
    <%= f.label :title -%>
    <%= f.text_field :title, :class => 'larger widest' -%>
  </div>

  <div class='field'>
    <%= f.label :slug -%>
    <%= f.text_field :slug, :class => 'larger widest' -%>
  </div>

  <%= render '/refinery/admin/form_actions', :f => f,
             :continue_editing => false,
             :delete_title => t('delete', :scope => 'refinery.marketing.admin.landing_pages.landing_page'),
             :delete_confirmation => t('message', :scope => 'refinery.admin.delete', :title => @landing_page.title) -%>
<% end -%>

<% content_for :javascripts do -%>
  <script>
    $(document).ready(function(){
      page_options.init(false, '', '');
    });
  </script>
<% end -%>
