<h1><%= @page.title %></h1>

<div class="row">
  <div class="medium-8 columns">
    <% if @page.children.any? %>
      <%= render 'refinery/pages/nested_pages', child_pages: @page.children %>
    <% else %>
      <p class="italic">Not sub-pages.</p>
    <% end %>
  </div>

  <div class="medium-4 columns">

  </div>
</div>
