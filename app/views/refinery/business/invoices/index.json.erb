{
  "data": <%= @invoices.includes(:company).to_json(only: [:id, :invoice_number, :invoice_type, :invoice_date, :due_date, :status, :reference, :currency_code, :total_amount, :amount_paid, :amount_due], include: { company: { only: [:name] } }).html_safe %>
}
